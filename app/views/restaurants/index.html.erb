<h1>All found restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to(restaurant.name, restaurant_path(restaurant)) %> (<%= restaurant.category %>)</li>
  <% end %>
</ul>

<%= link_to "Create Restaurant", new_restaurant_path %>
